meta {
  name: Create Dungeon - Invalid (No Size)
  type: http
  seq: 4
}

post {
  url: http://localhost:4003/api/v1/dungeon
  body: json
  auth: none
}

body:json {
  {
    "difficulty": "easy"
  }
}

tests {
  test("status should be 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("should return error message", function() {
    expect(res.body.error).to.include('size');
  });
}
